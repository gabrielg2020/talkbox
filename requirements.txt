# Core TUI
questionary
rich
pyfiglet

# Engines
gtts            # fast, networked, no timing
kokoro          # local, offline, native per-word timestamps (needs Python 3.12)
soundfile       # write Kokoro audio to wav

# Document loaders
markdown        # .md → HTML
beautifulsoup4  # strip HTML to plain text
python-docx     # .docx paragraph text

# Read-along playback (live, pitch-corrected speed)
python-mpv      # needs the system mpv/libmpv installed, e.g. `sudo pacman -S mpv`

# CPU-only torch keeps the Kokoro install ~200MB instead of pulling CUDA wheels:
#   uv pip install torch --index-url https://download.pytorch.org/whl/cpu
